name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: bazelbuild/setup-bazelisk@v1
      - run: echo '{"credHelpers":{"ghcr.io":"ghcr"}}' > ~/.docker/config.json
      - run: echo 'echo "{"Secret":"${{ secrets.GITHUB_TOKEN }}"}"' > /usr/bin/docker-credential-ghcr
      - run: bazel test //...
