name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: bazelbuild/setup-bazelisk@v1
      - run: echo '{"credHelpers":{"ghcr.io":"ghcr"}}' > ~/.docker/config.json
      - run: mkdir -p ~/bin
      - run: echo 'echo "{"ServerURL":"ghcr.io","Username":"Bearer","Secret":"${{ secrets.GITHUB_TOKEN }}"}"' > ~/bin/docker-credential-ghcr
      - run: chmod +x ~/bin/docker-credential-ghcr
      - run: PATH="~/bin:$PATH" bazel build //cmd/ocitool:ocitool && cp bazel-bin/cmd/ocitool/ocitool_/ocitool bin/ocitool-linux-amd64
      - run: PATH="~/bin:$PATH" bazel test //...
