load("@rules_pkg//pkg:pkg.bzl", "pkg_tar")

pkg_tar(
    name = "release",
    srcs = [
        "//oci:files",
        "//oci/private:files",
        "//oci/private/repositories:files",
    ],
    empty_files = ["BUILD.bazel"],
    extension = "tar.gz",
)
